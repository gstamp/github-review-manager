import SwiftUI

struct ApproveIcon: View {
    var body: some View {
        Image(systemName: "checkmark.circle.fill")
            .font(.system(size: 14))
            .foregroundColor(.green)
    }
}

